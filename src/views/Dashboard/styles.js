import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  buttonSignIn: {
    width: 300,
    height: 50,
    borderRadius: 8,
    borderWidth: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#213993',
  },
  buttonSignInText: {
    fontSize: 28,
    color: '#EEE',
  },
});

export default styles;
